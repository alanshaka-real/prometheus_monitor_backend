# 后端历史变更记录

## v1.0.12 (2026-02-17)

### 安全加固

- **JWT httpOnly Cookie**：`AuthController.login()` 设置 `Set-Cookie: access_token=xxx; HttpOnly; Secure; SameSite=Lax; Path=/api`；`JwtAuthenticationFilter` 同时支持 Authorization Header 和 httpOnly Cookie 双模式
- **用户登出**：新增 `POST /api/auth/logout` 端点清除 httpOnly Cookie
- **WebSocket Token 清理**：`WebSocketAuthInterceptor` 移除 URL 参数 token 接受，仅支持消息认证

### 告警引擎

- **告警规则评估调度器**：新增 `AlertEvaluationScheduler`（@Scheduled fixedRate 60s），查询所有启用规则，执行 PromQL 即时查询，触发告警创建 PromAlertHistory 记录
- **PromQL 远程验证**：`AlertServiceImpl.validateOnPrometheus()` 调用 Prometheus 即时查询验证语法，不可用时降级为本地检查
- **Alertmanager Webhook**：新增 `POST /api/prometheus/alert/webhook` 接收 Alertmanager 通知
- **静默规则执行**：告警评估时检查活跃静默规则，匹配则抑制通知，标记 `silenced=true`；自动过期已结束的静默规则
- **告警收敛/降噪**：新增 `AlertGroupManager`，按 groupName:severity 分组，实现 group_wait(30s)/group_interval(5min)/repeat_interval(4h) 三级时间窗控制

### 性能优化

- **Spring Cache + Caffeine**：`CacheConfig` 配置缓存管理器，`@Cacheable` 应用于权限树(5min)、仪表盘模板树(10min)、PromQL模板树(10min)
- **SSH 连接池**：`SshConnectionPool`（commons-pool2 KeyedObjectPool，按 host:port:username 分组，maxTotal=20, maxPerKey=3, minEvictableIdle=5min）
- **乐观锁**：prom_dashboard/alert_rule/instance/exporter 添加 `@Version` 字段 + `OptimisticLockerInnerInterceptor`
- **事务隔离**：`PermissionServiceImpl` updateUser/updateRole 使用 `REPEATABLE_READ` 隔离级别
- **响应格式统一**：`PageResult` 添加 `@JsonProperty("list"/"pageNum"/"pageSize")` 别名

### 调度器

- **实例健康检查**：`InstanceHealthScheduler` 每 5 分钟对每个实例调用 `GET /-/healthy`，更新 status 为 online/offline
- **指标元数据同步**：`MetadataScheduler` 每 30 分钟从在线 Prometheus 实例拉取 `/api/v1/metadata`，upsert 到 prom_metric_meta 表

### 高级功能

- **Grafana 仪表盘导入**：`POST /api/prometheus/dashboards/import` 接受 Grafana JSON，自动转换面板类型和 PromQL targets
- **Grafana 仪表盘导出**：`GET /api/prometheus/dashboards/{id}/export?format=grafana` 导出 Grafana 兼容 JSON（含 __inputs/__requires/targets/gridPos）
- **集群拓扑 WebSocket**：`ClusterTopologyWebSocketHandler` 注册 `/ws/cluster-topology`，定时广播拓扑更新

### 数据库迁移

- **V10__add_optimistic_locking.sql**：prom_dashboard/alert_rule/exporter 添加 `version INT DEFAULT 0`；prom_instance 添加 `opt_version INT DEFAULT 0`；prom_alert_history 添加 `silenced TINYINT(1) DEFAULT 0`
- **V11__add_foreign_keys.sql**：清理孤儿记录，添加 CASCADE 外键（sys_user_role/sys_role_permission/prom_workspace_dashboard）

### 新建文件
- `[新建] module/alert/scheduler/AlertEvaluationScheduler.java` — 告警规则定时评估调度器
- `[新建] module/alert/scheduler/AlertGroupManager.java` — 告警收敛/降噪管理器
- `[新建] module/datasource/scheduler/InstanceHealthScheduler.java` — 实例健康检查调度器
- `[新建] module/datasource/scheduler/MetadataScheduler.java` — 指标元数据同步调度器
- `[新建] module/distribute/service/SshConnectionPool.java` — SSH 连接池
- `[新建] config/CacheConfig.java` — Spring Cache + Caffeine 配置
- `[新建] websocket/ClusterTopologyWebSocketHandler.java` — 集群拓扑 WebSocket 处理器
- `[新建] db/migration/V10__add_optimistic_locking.sql` — 乐观锁字段迁移
- `[新建] db/migration/V11__add_foreign_keys.sql` — 外键约束迁移

### 变更文件
- `[修改] module/auth/controller/AuthController.java` — httpOnly Cookie 设置、logout 端点
- `[修改] security/JwtAuthenticationFilter.java` — 支持 Cookie + Header 双模式认证
- `[修改] config/SecurityConfig.java` — permitAll 添加 logout/webhook
- `[修改] websocket/WebSocketAuthInterceptor.java` — 移除 URL 参数 token
- `[修改] config/WebSocketConfig.java` — 注册 /ws/cluster-topology
- `[修改] module/alert/service/impl/AlertServiceImpl.java` — 远程 PromQL 验证
- `[修改] module/alert/controller/AlertController.java` — Alertmanager webhook 端点
- `[修改] module/dashboard/service/DashboardService.java` — 新增 Grafana 导入/导出接口
- `[修改] module/dashboard/service/impl/DashboardServiceImpl.java` — Grafana JSON 转换实现
- `[修改] module/dashboard/controller/DashboardController.java` — 导入/导出端点
- `[修改] module/permission/service/impl/PermissionServiceImpl.java` — @Cacheable + REPEATABLE_READ
- `[修改] config/MybatisPlusConfig.java` — OptimisticLockerInnerInterceptor
- `[修改] common/page/PageResult.java` — list/pageNum/pageSize JSON 别名
- `[修改] module/datasource/entity/PromInstance.java` — optVersion 乐观锁字段
- `[修改] module/dashboard/entity/PromDashboard.java` — @Version
- `[修改] module/alert/entity/PromAlertRule.java` — @Version
- `[修改] module/datasource/entity/PromExporter.java` — @Version
- `[修改] module/alert/entity/PromAlertHistory.java` — silenced 字段
- `[修改] pom.xml` — 添加 spring-boot-starter-cache/caffeine/commons-pool2
- `[修改] application-dev.yml` — 添加 alert/instance/metadata/cluster 调度器配置

## v1.0.11 (2026-02-16)

### 功能 — 部署安装：支持非 Prometheus 组件配置参数

后端支持从部署任务中提取各 Exporter 组件的配置参数，生成对应的 CLI 启动参数、配置文件和 systemd 服务。

**新增功能：**
- `ComponentConfigDTO`：扁平 DTO，含所有组件的通用字段（installDir/port/logLevel）和组件特有字段
- `ComponentConfigGenerator` 工具类：
  - `buildExecStart()` — 按组件类型拼接 CLI 参数（node_exporter 的 --no-collector、cadvisor 的 --port=、redis_exporter 的 --redis.addr 等）
  - `generateBlackboxYaml()` — 默认 blackbox.yml（http_2xx/http_post_2xx/tcp_connect/icmp 四个探测模块）
  - `generateMyCnf()` — MySQL .my.cnf 凭据文件
  - `generateSystemdService()` — 完整 systemd unit 文件（MySQL 额外含 Environment=DATA_SOURCE_NAME）
- `DistributeTaskExecutor` 集成：
  - `extractComponentConfigs()` 从 task.config 提取 `Map<String, ComponentConfigDTO>`
  - installDir/port 优先取组件配置
  - blackbox_exporter 自动写入 blackbox.yml
  - mysql_exporter 自动写入 ~/.my.cnf（chmod 600）
  - totalSteps 计算：blackbox/mysql 额外 +1 步
  - 移除旧的硬编码 `generateSystemdService()` 方法

### 变更文件
- `[新建] module/distribute/dto/ComponentConfigDTO.java` — 组件配置扁平 DTO
- `[新建] module/distribute/util/ComponentConfigGenerator.java` — 组件配置生成工具类
- `[修改] module/distribute/service/impl/DistributeTaskExecutor.java` — 集成组件配置，替换旧方法

## v1.0.10 (2026-02-16)

### 修复 — 代码审查问题修复

#### 数据库 Schema 漂移修复
- **prom_promql_template 表补齐 5 个字段**：`sub_category`、`exporter_type`、`unit`、`unit_format`、`sort_order`，与 Entity `PromPromqlTemplate.java` 对齐
  - 更新 `src/main/resources/db/schema.sql` CREATE TABLE 语句
  - `docs/schema.sql` 已在先前版本中包含这些字段，无需修改
  - 线上执行 `ALTER TABLE prom_promql_template ADD COLUMN ...` 补齐

#### EmailSender 实现真实邮件发送
- **接入 JavaMailSender**：`EmailSender.java` 从 TODO 桩代码升级为真实邮件发送
  - 使用 `@Autowired(required = false)` 注入 `JavaMailSender`，未配置 SMTP 时降级为打日志
  - 已配置时使用 `MimeMessageHelper` 发送 HTML 格式邮件，支持 to/cc/from/subject 参数
  - `spring-boot-starter-mail` 依赖已存在于 pom.xml
- **application.yml** 新增 Spring Mail 配置项（默认注释状态）

### 变更文件
- `[修改] src/main/resources/db/schema.sql` — prom_promql_template 补齐 5 个字段
- `[修改] module/alert/notification/EmailSender.java` — 接入 JavaMailSender 实现真实邮件发送
- `[修改] src/main/resources/application.yml` — 新增 spring.mail 配置（注释状态）

## v1.0.9 (2026-02-16)

### 功能 — 软件包管理：在线更新 + 离线上传

- **在线更新（GitHub Releases）**：新增 `POST /api/prometheus/distribute/software/download` 端点，支持从 GitHub Releases 自动获取 8 个组件最新版本并下载到本地，通过 WebSocket 实时推送下载进度
  - 支持的组件：Prometheus、Node Exporter、Blackbox Exporter、Process Exporter、MySQL Exporter、Redis Exporter、Nginx Exporter、cAdvisor
  - 每个组件按各自的命名规则转换并统一为 `{name}-{version}.{os}-{arch}.tar.gz` 格式
  - 下载完成后自动清理旧版本文件和数据库记录，注册新版本
  - 支持可选 GitHub Token 配置以提升 API 限额（60/h → 5000/h）
- **下载进度查询**：新增 `GET /api/prometheus/distribute/software/download/{downloadId}` 端点，轮询获取下载状态作为 WebSocket 兜底
- **离线上传**：新增 `POST /api/prometheus/distribute/software/upload` 端点，支持手动上传 tar.gz 文件
  - 自动解析文件名、校验组件名、保存文件、清理旧版本并注册新记录
- **文件上传限制**：application.yml 配置 multipart max-file-size/max-request-size 为 500MB

### 新建文件
- `[新建] module/distribute/config/GitHubReleaseConfig.java` — 8 个组件与 GitHub 仓库的映射关系、下载 URL 构建
- `[新建] module/distribute/service/impl/SoftwareDownloadExecutor.java` — 异步下载执行器，WebSocket 进度推送
- `[新建] module/distribute/vo/SoftwareDownloadVO.java` — 下载进度/结果 VO
- `[新建] module/distribute/vo/SoftwareUploadVO.java` — 上传结果 VO

### 变更文件
- `[修改] module/distribute/service/SoftwareService.java` — 新增 downloadLatest、getDownloadStatus、uploadSoftware 接口
- `[修改] module/distribute/service/impl/SoftwareServiceImpl.java` — 实现 3 个新方法
- `[修改] module/distribute/controller/DistributeController.java` — 新增 3 个端点
- `[修改] src/main/resources/application.yml` — 添加 multipart 文件大小限制
- `[修改] src/main/resources/application-dev.yml` — 添加 github-token 配置项

## v1.0.8 (2026-02-15)

### 功能 — 指标元数据扩充 + 收藏切换端点
- **指标元数据扩充（24 → 119 条）**：`schema.sql` 新增 95 条 `INSERT IGNORE` 语句，覆盖 8 种 Exporter 类型的常用运维指标
  - Node Exporter (+25)：磁盘 I/O、网络错误/丢包、内存细节、文件描述符、inode、系统时间
  - Prometheus (+11)：TSDB 指标、Scrape 指标、Engine 查询耗时、配置重载状态、通知丢弃
  - cAdvisor (+9)：CPU 细分、working_set 内存、文件系统使用、容器网络
  - MySQL (+13)：线程运行数、最大连接数、慢查询、字节收发、InnoDB buffer pool、主从延迟
  - Redis (+13)：命令处理 OPS、keyspace 命中率、内存上限/RSS、从节点、RDB、阻塞/拒绝/驱逐
  - Nginx (+8)：连接状态（accepted/handled/reading/writing/waiting）、HTTP 响应、上游延迟
  - Blackbox (+8)：HTTP 状态码/内容、SSL 证书过期、分阶段延迟、DNS、重定向、ICMP
  - Process (+8)：CPU、内存、文件描述符、FD 比率、进程数、线程数、读/写字节
- **新增收藏切换端点**：`POST /api/prometheus/metrics/metadata/{id}/favorite`，切换指标的 `favorite` 布尔状态

### 变更文件
- `[修改] src/main/resources/db/schema.sql` — 追加 95 条指标元数据
- `[修改] module/datasource/service/DatasourceService.java` — 新增 `toggleMetricFavorite` 接口方法
- `[修改] module/datasource/service/impl/DatasourceServiceImpl.java` — 实现收藏切换逻辑
- `[修改] module/datasource/controller/DatasourceController.java` — 新增收藏切换端点

## v1.0.7 (2026-02-15)

### 功能 — Exporter 批量导入（从实例配置）
- **批量导入端点**：新增 `POST /api/prometheus/exporters/batch`，接收 `BatchCreateExporterDTO`（含 instanceId + exporters 列表），返回 `BatchCreateResultVO`（total/created/skipped/skippedTargets）
- **去重逻辑**：以 `host:port` 为唯一键，查询已有 Exporter 构建 Set 进行 O(1) 查重，同时防止批内重复
- **默认值填充**：interval 为空时默认 `"15s"`，metricsPath 为空时默认 `"/metrics"`，status 固定为 `"stopped"`
- **事务保证**：`@Transactional` 注解保证批量插入的原子性

### 变更文件
- `[新增] module/datasource/dto/BatchCreateExporterDTO.java` — 批量导入请求 DTO
- `[新增] module/datasource/vo/BatchCreateResultVO.java` — 批量导入结果 VO
- `[修改] module/datasource/service/DatasourceService.java` — 新增 batchCreateExporters 接口方法
- `[修改] module/datasource/service/impl/DatasourceServiceImpl.java` — 实现批量导入逻辑
- `[修改] module/datasource/controller/DatasourceController.java` — 新增批量导入端点

## v1.0.6 (2026-02-15)

### 修复 — 配置推送写入旧内容（rawYaml 覆盖结构化配置）
- **rawYaml 优先级 Bug**：`PrometheusYamlGenerator.generate()` 优先使用 `rawYaml` 字段，导致用户在结构化表单中的所有修改被忽略，推送的 YAML 始终是初次 SSH 读取的原始内容
  - 修复：`pushInstanceConfig()` 在生成 YAML 前，若存在结构化 `scrapeConfigs` 则清除 `rawYaml`
  - 修复：保存 configJson 到 DB 时始终清除 `rawYaml`，防止后续加载时再次覆盖
- **前端同步修复**：加载配置和读取远程配置时清除 `rawYaml`；推送配置时从请求体中剥离 `rawYaml`
- **writeRemoteFile 不检查退出码**：SSH 写文件命令失败时被静默吞掉，不读 stderr
  - 修复：添加退出码检查和 stderr 读取，失败时抛出异常
- **空 target 过滤**：scrapeConfigs 中的空字符串 target（如 `["192.168.0.100:9090",""]`）在生成 YAML 前被自动过滤

### 变更文件
- `[修改] module/datasource/service/impl/DatasourceServiceImpl.java` — 清除 rawYaml、过滤空 target、writeRemoteFile 退出码检查

## v1.0.5 (2026-02-15)

### 功能 — Prometheus 实例配置管理
- **数据库扩展**：`prom_instance` 表新增 `machine_id`（关联分发机器）、`config_json`（配置 JSON）、`lifecycle_enabled`（热加载标志）3 个字段
- **实体扩展**：`PromInstance` 新增 `machineId`、`configJson`（JacksonTypeHandler）、`lifecycleEnabled` 字段
- **Service 层新增 5 个方法**：
  - `getInstanceConfig()` — 优先从 DB 读取配置 JSON，其次 SSH 读取远程 prometheus.yml
  - `pushInstanceConfig()` — 生成 YAML + SSH 写入 + 可选热加载
  - `uploadInstanceConfig()` — 上传自定义 YAML 配置文件
  - `reloadInstance()` — POST /-/reload 热加载 Prometheus
  - `linkMachine()` — 关联/解绑分发机器，自动检测 lifecycle 启动参数
- **Controller 新增 6 个端点**：获取配置、推送配置、上传 YAML、热加载、关联机器、解绑机器
- **SSH 工具方法**：复用分发模块的 SSH 连接、命令执行、密码解密模式

### 变更文件
- `[修改] src/main/resources/db/schema.sql` — prom_instance 新增 3 个字段
- `[修改] module/datasource/entity/PromInstance.java` — 新增 3 个字段
- `[修改] module/datasource/service/DatasourceService.java` — 新增 5 个接口方法
- `[修改] module/datasource/service/impl/DatasourceServiceImpl.java` — 实现配置管理 + SSH 工具方法
- `[修改] module/datasource/controller/DatasourceController.java` — 新增 6 个 API 端点

## v1.0.4 (2026-02-15)

### 功能 — 检测报告重新设计
- **新增组件检测**：`detectComponents(SSHClient)` 方法检测 6 个组件（Prometheus、Node Exporter、Alertmanager、Blackbox Exporter、Pushgateway、Grafana）的安装路径、版本、运行状态、端口
- **MachineDetectVO 扩展**：新增 `machineId`/`machineName`/`machineIp` 机器标识 + `components` 组件列表
- **批量检测失败携带机器标识**：`batchDetect()` catch 块中的失败 VO 也设置 machineId/machineName/machineIp
- **修复 execCmd() join 顺序**：`cmd.join()` 移到 `readFully()` 之前（与 DistributeTaskExecutor 中的修复一致）

### 修复 — 批量检测返回格式不匹配
- **Controller 返回裸数组**：`batchDetect()` 返回 `R.ok(List<MachineDetectVO>)` 即 `{data: [...]}`，前端期望 `{data: {list: [...], total: N}}`
  - 修复：Controller 改为包装成 `Map<String, Object>` 含 `list` 和 `total` 键，与 `listMachines` 等其他列表接口保持一致

### 变更文件
- `[新增] module/distribute/vo/ComponentDetectVO.java`
- `[修改] module/distribute/vo/MachineDetectVO.java`
- `[修改] module/distribute/service/impl/DistributeServiceImpl.java`
- `[修改] module/distribute/controller/DistributeController.java`

## v1.0.3 (2026-02-15)

### 修复 — 实例管理字段映射 + 连接测试错误信息
- `PromInstance.groupName` 添加 `@JsonProperty("group")` 使 JSON 字段名与前端一致
- `ConnectionTestVO` 新增 `message` 字段，连接失败时返回具体异常信息

### 变更文件
- `[修改] module/datasource/entity/PromInstance.java`
- `[修改] module/datasource/vo/ConnectionTestVO.java`
- `[修改] module/datasource/service/impl/DatasourceServiceImpl.java`

## v1.0.2 (2026-02-15)

### 修复 — SSH 命令执行顺序 + systemd 服务文件写入
- `execCmd()`/`execCmdChecked()`: `cmd.join()` 移到 `readFully()` 之前，确保命令完成后再读取输出
- `execCmdChecked()`: null 退出码（超时）现在抛异常而非静默通过
- 新增 `writeSudoFile()`: 替代有缺陷的 `sudo -S tee` 管道写法，使用临时文件 + `sudo cp`
- `execCmdWithSudo()`: 支持自定义超时参数，tar 解压使用 120 秒超时
- 解压后新增文件列表验证

### 变更文件
- `[修改] module/distribute/service/impl/DistributeTaskExecutor.java`

## v1.0.1 (2026-02-15)

### 修复 — 分发部署安装 Bug

**Bug 1: Prometheus Server 安装失败**
- `@Async` 自调用失效：`createTask()` 在同一个 Bean 内调用 `executeTaskAsync()`，Spring AOP 代理被绕过
- `dataDir` 路径 `~` 未解析：systemd 不展开 `~`，Prometheus 启动失败
- 非 Prometheus 组件 systemd 服务文件使用 `\\n` 字面量，文件格式无效
- 配置验证命令 `--check` 改为 `promtool check config`

**Bug 2: 安装日志不显示**
- `updateProgress()` 未调用 `sendLog()`，前端 WebSocket 日志面板无数据

### 变更文件
- `[新增] module/distribute/service/impl/DistributeTaskExecutor.java`
- `[重构] module/distribute/service/impl/DistributeServiceImpl.java`

## v1.0.0 (2026-02-14)

### 初始版本
- Spring Boot 3.2.5 + Java 17
- 9 个功能模块（auth, datasource, dashboard, query, alert, cluster, permission, settings, distribute）
- JWT 认证 + RBAC 权限
- MyBatis-Plus ORM
- WebSocket 实时通信
- Knife4j API 文档
